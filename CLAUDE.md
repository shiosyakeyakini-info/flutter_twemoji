# CLAUDE.md

## What this repository is

A fork of [jasonlessenich/flutter_twemoji](https://github.com/jasonlessenich/flutter_twemoji) whose
Twemoji assets track [jdecked/twemoji](https://github.com/jdecked/twemoji), the fork that keeps
following Unicode now that twitter/twemoji is dormant. The Flutter widget layer is upstream's; what
this fork adds is the tooling that keeps the assets and the emoji regex current.

It is maintained for Misskey clients. The API is not Misskey-specific, but where a choice had to be
made it went to how a Misskey server hands emoji out — which is why the emoji regex comes from
`@misskey-dev/emoji-data` (see 絵文字正規表現の出どころ below) rather than from `@twemoji/parser`.

## Commands

```bash
flutter pub get
flutter test                      # asset/regex consistency checks
flutter analyze --no-fatal-infos  # infos are pre-existing (deprecations, line length)

tool/sync_twemoji.sh                # sync with the newest upstream release
tool/sync_twemoji.sh --ref v17.0.3  # sync with a specific release
tool/sync_twemoji.sh --print-current
```

## Generated files — do not edit by hand

Run `tool/sync_twemoji.sh` instead; editing these by hand is silently undone by the next sync.

- `assets/png/`, `assets/svg/` — copied from the upstream `assets/72x72` and `assets/svg`, plus one
  alias: the eye in speech bubble is stored under both of its file names (see 絵文字正規表現の出どころ
  below), which is why the bundle holds 4010 files where upstream ships 4009
- `lib/src/twemoji_version.dart` — records the upstream repository and release
- the `// twemoji-regex:begin`…`:end` block in `lib/src/utils.dart` — `TwemojiUtils.emojiRegex`,
  generated by `tool/update_emoji_regex.mjs` from the `@misskey-dev/emoji-data` npm package, which
  builds its pattern from Twemoji's `emoji.yml` (see 絵文字正規表現の出どころ below)

Assets and regex have to cover the same emoji set: syncing assets alone leaves new emoji unmatched
by `TwemojiText`, and `flutter test` fails when the two drift apart. The regex is embedded in a
Dart string that resolves the `\uXXXX` escapes before `RegExp` sees them, so the generator rejects
any upstream pattern that would need different escaping rather than emitting broken Dart, and it
refuses to commit a pattern that has lost any of the properties this fork picked its source for
(unqualified emoji, skin tones on text-default emoji, U+FE0E still rejected).

The two are versioned separately. `--twemoji-version` selects the newest `@misskey-dev/emoji-data`
release of the same major.minor series (17.0.x for Twemoji 17.0.x): the major.minor tracks the
Unicode/Twemoji generation, the patch is the package's own revision and moves independently of
Twemoji's. `--data-version` pins an exact one.

## 絵文字正規表現の出どころ

アセットと正規表現は別々のところから来ていて、揉めるのは正規表現のほうだけである。本家
`flutter_twemoji` 1.1.0 とこのフォークとでアセットは純増（+163ファイル、削除ゼロ）なので、
壊れたものがあるとすれば原因は正規表現にしかない。

**2026-08 に生成元を `@twemoji/parser` から `@misskey-dev/emoji-data/regex` に切り替えた。**
以下はその経緯と、切り替え時に測った数字である。

上流で起きたことを順に（一覧は README の「上流の経緯」にある）:

- 2026-06-01 — [jdecked/twemoji-parser#12](https://github.com/jdecked/twemoji-parser/pull/12) が
  `Emoji_Presentation=No` の文字をすべてテキスト既定に変更。パーサ 17.0.2 で公開され、これらの
  文字は U+FE0F が続くときしかマッチしなくなった。それ以前の生成物はテキスト既定のグループの
  後ろに `(?:\uFE0F|(?!\uFE0E))` を付けており、1.1.0 が素の `☹` を拾えていたのはそのため。
- 2026-06-18 — Misskey が以前の挙動を選択肢として残すよう要望
  （[#13](https://github.com/jdecked/twemoji-parser/issues/13)）。not planned で終了。
- 2026-06-28 — Misskey は代わりに Twemoji の `emoji.yml` から自前で正規表現を生成する道を選ぶ
  （[misskey-dev/emojis#9](https://github.com/misskey-dev/emojis/pull/9)）。
  `@misskey-dev/emoji-data/regex` として公開されている。
- 2026-07-07 — [#16](https://github.com/jdecked/twemoji-parser/issues/16)。パーサのメンテナ自身が
  立てたもので、テキスト既定かつ肌の色を取れる10字（261d 270c 270d 1f574 1f575 1f590 26f7 26f9
  1f3cb 1f3cc）が完全修飾してもまったくマッチしない、という内容。未修正、PR もなし。

Misskey が配っている Unicode 絵文字1915字（`@misskey-dev/emoji-data` 17.0.6）に対して、`Twemoji`
とまったく同じ手順（正規表現にかけ、一致部分を `toUnicode` に通し、そのアセットを探す）で数えた結果:

| 正規表現 | 描画できない数 |
|---|---|
| 旧: `@twemoji/parser` 17.0.2 | 141 = U+FE0F 待ちの130 + #16 の10 + `👁️‍🗨️` |
| 新: `@misskey-dev/emoji-data` 17.0.6 | 1 = `👁️‍🗨️` のみ。アセットの別名で解消して 0 |
| 本家 1.1.0（旧 `twitter/twemoji-parser`） | 古い正規表現が知らない Unicode 16/17 の14字。うち7字は 1.1.0 の時点でアセットは同梱されていた |

`👁️‍🗨️` だけは正規表現とは無関係で、アセット名が `1f441-200d-1f5e8.svg` なのに対し `toUnicode` は
ZWJ があるときは U+FE0F を残すため、名前が一致しない。ZWJ 列でも U+FE0F を残すアセット名
（`1f3cb-fe0f-200d-2640-fe0f` など）のほうが多数派なので、`toUnicode` 側を一律に変えると他が壊れる。
そこで `tool/sync_twemoji.sh` の `alias_eye_in_speech_bubble` が、この1字だけアセットを両方の名前で
置く。上流がどちらの名前に倒しても成り立つよう向きを判定してからコピーする。Misskey も同じことを
`packages/emoji-assets/scripts/build.ts` の「3.5. Twemojiの目の絵文字修正」でやっていて、正規表現側でも
`additionalEmojiSequencesByUnicode` で素の `1f441-200d-1f5e8` をエイリアスとして足している。どちらも
[jdecked/twemoji#151](https://github.com/jdecked/twemoji/issues/151)（2025-12-23 起票、open）が決着する
までの措置である。**これで1915字すべてが描画できる。**

パターンを手で直す道が取れなかった理由も残しておく:

- U+FE0F をすべて任意にすると正規表現ごと壊れる。パターンの末尾は `|\uFE0F` という単独の選択肢に
  なっていて、そこまで書き換えると空文字にマッチする選択肢ができ、`splitMapJoin` が至るところで
  幅ゼロの一致を拾って、どの絵文字も `''` に解決されてしまう。
- 末尾を残せば141は11まで減るが、#16 の10字は U+FE0F の有無で制御されているのではなく、そもそも
  パターンに存在しない。文字列置換では届かない。

`@misskey-dev/emoji-data` を選ぶにあたって承知しておくこと:

- 出どころは `@twemoji/parser` と同じ Twemoji の `emoji.yml` で、正規表現部分のライセンスも
  同じ MIT（パッケージの LICENSE に `Emoji regex by Twemoji (@twemoji/parser)` として明記）。
  埋め込む文字列の権利関係は切り替え前後で変わらない。
- README に「ほかのプロダクトで使用されることは想定していません」と書かれている。バージョン方針も
  Twemoji のリリースとは独立に動く（17.0.0〜17.0.3 は古いパーサの正規表現をそのまま同梱していて、
  自前生成に変わったのは 17.0.5 から）。だから `tool/update_emoji_regex.mjs` は
  major.minor だけを合わせて最新を取り、生成した正規表現が期待する性質を失っていないか
  （素の絵文字・肌の色つきのテキスト既定絵文字・U+FE0E の拒否）を確かめてからでないと書き込まない。
  同じ性質は `test/flutter_twemoji_test.dart` でも押さえてある。
- 上流が消えたり方針を変えたりした場合の退避先は、`emoji.yml` から自前で生成するジェネレータを
  ここに書くこと。切り替えても壊れないよう、生成物の形（Dart 文字列に埋め込める `\uXXXX` のみの
  パターン）とチェックはジェネレータ側に閉じている。

正規表現とは別の話として、`Twemoji.build` はパターンが一致しないと `SizedBox.shrink()` を返す。
取りこぼしが代替グリフではなく見えない隙間になるのはこのためである。いまは 1915/1915 が通っているので
急ぐ理由はないが、`toUnicode` が1対1の変換である以上、アセット名との食い違いは今回のような別名対応で
1件ずつ潰すことになる。件数が増えるようなら、入力そのもの → 入力 + U+FE0F → 素のコードポイント、の順に
試し、同梱アセット名の一覧（生成する）と突き合わせる解決器に寄せたほうが早い。

## Layout

- `lib/src/twemoji_widget.dart` — `Twemoji`, a single emoji as an SVG or 72x72 PNG asset
- `lib/src/twemoji_text.dart`, `twemoji_text_span.dart` — text with inline emoji, split via
  `TwemojiUtils.emojiRegex`
- `lib/src/utils.dart` — the regex plus `toUnicode`, which maps an emoji to its asset file name
- `bin/include_emojis.dart` — `dart run flutter_twemoji:include_emojis`, filters the bundled assets
  down to the emoji listed under `flutter_twemoji: includes:` in the consumer's pubspec
- `example/` — demo app

## Workflows

- `.github/workflows/sync-twemoji.yml` — manual (`workflow_dispatch`) only, opens a pull request
  with the sync result. Deliberately not on a schedule: upstream publishes a few releases a year,
  and GitHub disables scheduled workflows in public repositories after 60 days without a commit.
- `.github/workflows/ci.yml` — `flutter analyze` and `flutter test`

## Releasing

`pubspec.yaml` `version` and a `CHANGELOG.md` entry are bumped by hand, not by the sync. The
package name still collides with the upstream package on pub.dev, so this fork is meant to be
consumed as a git dependency unless it is renamed first.
